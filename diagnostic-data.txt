Watch Player + Sync Diagnostic Report
Generated: 2026-03-10 (America/Chicago)
Workspace: C:\Users\reidm\bilm

Broken / At Risk
1) Third-party streaming providers remain the top runtime risk.
- Domains: `vidsrc-embed.ru`, `multiembed.mov`, `embedmaster.link`, `www.vidking.net`, `vidnest.fun`.
- Impact: playback can fail even with correct local routing if upstream providers are blocked, throttled, or down.

2) TMDB proxy fallback is intentionally env-gated.
- `TMDB_API_KEY` is now server-only and required for `/api/tmdb/*`.
- Impact: when the env var is missing, TMDB fallback routes return unavailable by design.

3) Cross-origin iframe command acceptance is provider-dependent.
- EmbedMaster `color1` dispatch now sends multiple safe formats with staged retries.
- Impact: if provider-side API behavior changes, runtime color sync can still degrade.

Potential Future Issues
1) Provider health memory may over-skip in short outage windows.
- Current TTL is short-lived and self-clearing, but aggressive fail caching can temporarily avoid a provider that already recovered.

2) Dual-cloud drift repair is best-effort.
- Drift guard and one-shot repair are in place, but major upstream API behavior changes can still require manual intervention.

3) Smoke automation is focused, not exhaustive.
- Playwright smoke checks cover core desktop/mobile paths, but deep edge cases still need targeted tests.

Working As Expected
1) Anime/non-anime identity routing is provider-aware and canonical.
- Keys and matching use provider+type IDs.
- Anime routes preserve `anime=1&aid=...&type=...` and no longer collapse into TMDB `id` routes.

2) Player provider cleanup and stack updates are applied.
- `superembed` and `godrive` are removed from active player flow.
- `VidKing` is available for non-anime movie/TV routes with accent color support.
- `se_player.php` and `embedmaster_verify.txt` are removed.

3) Subtitle behavior is isolated correctly.
- Anime watch pages keep subtitles disabled (`off`) and do not mutate subtitle params.
- Non-anime subtitle params are VidSrc-only via `ds_lang=en|es|fr`.

4) Player bar interaction reliability is improved.
- `Language`, `Subtitles`, `Autoplay`, and `Server` menus are mutually exclusive.
- Menu click propagation is controlled to avoid overlap/reopen edge cases.

5) EmbedMaster accent sync is hardened.
- `color1` dispatch runs on iframe load, server switch to EmbedMaster, and theme accent changes.
- Staged retries are included for slower device/player boot timing.

6) Retry false-failure handling is improved.
- Iframe retries use grace windows and late-load reconciliation.
- Load-event reconciliation helps prevent false error states on slower systems.

7) Daily Firebase mirror guard is active.
- Snapshot mirroring is limited to at most once per 24 hours and does not block primary sync paths.

8) Migration quarantine and manual restore are available.
- Malformed migration entries are quarantined, not silently dropped.
- Settings includes local restore/clear controls for quarantine recovery.

9) Local diagnostics and smoke automation are in repo.
- Settings includes Connection Health checks for critical dependencies.
- Playwright smoke tests run for desktop and mobile core flows.
